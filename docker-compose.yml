services:
  easyproxy:
    image: ghcr.io/soutm70/easyproxy:latest
    container_name: easyproxy
    restart: unless-stopped
    ports:
      - "7861:7860"
    environment:
      #- API_PASSWORD=
      - PORT=7860
    volumes:
      - ./data:/data
